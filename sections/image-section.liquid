// ...existing code...

<img 
    src="{{ image.src }}" 
    alt="{{ image.alt }}" 
    class="responsive-image" 
    style="max-width: 100%; height: auto;" 
    loading="lazy" />
// Ensure no `{% if request.device_type == 'mobile' %}` or similar conditions hide this block

// ...existing code...
